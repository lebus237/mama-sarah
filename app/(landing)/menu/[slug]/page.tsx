/**
 *
 * @description This page exist only to meet SEO requirements.Actual menu dish display will be handled by the menu page.
 *
 * @todo Add redirection of dish slug to menu page
 * @todo Implement metadata
 */
export default function Page({ params }: { params: { slug: string } }) {
   return <div></div>
}
